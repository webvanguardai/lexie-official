import type { Metadata } from "next";
import { Playfair_Display, Inter } from "next/font/google";
import "./globals.css";

const playfair = Playfair_Display({
  subsets: ["latin"],
  variable: "--font-playfair",
  display: "swap",
});

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "LEXIE — Pop. Soul. Country. Dubai.",
  description:
    "LEXIE — Dubai-based pop/soul/country singer-songwriter. Formerly Warner Music & Island Records. Now independent.",
  openGraph: {
    title: "LEXIE — Pop. Soul. Country. Dubai.",
    description:
      "Dubai-based pop/soul/country singer-songwriter. Formerly Warner Music & Island Records.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className={`${playfair.variable} ${inter.variable}`}>
      <body className="font-sans antialiased">{children}</body>
    </html>
  );
}
